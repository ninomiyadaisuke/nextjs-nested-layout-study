import { FC } from 'react';

import styles from '@/styles/components/footer/footer.module.scss';

const Footer: FC = () => {
  return <footer className={styles.footer}>Footerです</footer>;
};

export default Footer;
